export default props => {
	const {
		font,
		fontSize,
		fontFamily,
		fontWeight,
		lineHeight,

		letterSpacing,
		textDecoration,
		textTransform
	} = props;

	return {
		font,
		fontSize,
		fontFamily,
		fontWeight,
		lineHeight,

		letterSpacing,
		textDecoration,
		textTransform
	};
};